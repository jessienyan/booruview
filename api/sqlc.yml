# After changing the queries or schema, run:
# $ sqlc generate

version: "2"
sql:
  - engine: "sqlite"
    queries: "database/queries.sql"
    schema: "database/schema.sql"
    gen:
      go:
        package: "models"
        out: "models"
        emit_db_tags: true
        emit_exact_table_names: true
